<div class="container">
   <% @articles.each do |article| %>
    <%= link_to article do %>
    <div class="col-md-4 margin-top-7per">
     <%= image_tag "no-image.jpg", class: "img-responsive"  %>
     <%= article.title %>
     <%= article.created_at.to_s(:datetime_jp) %>
   </div>
   <% end %>
 <% end %>
 <div class="row">
  <%= link_to "新規作成", new_article_path,class: "margin-top-10per btn btn-primary" %>
 </div>
</div>
